val textFile = sc.textFile("/home/rukith/wc.txt")

val counts = textFile
  .flatMap(line => line.split(" "))
  .map(word => (word, 1))
  .reduceByKey(_ + _)

val result = counts.filter(x => x._2 > 4)

result.collect().foreach(println)